import axios from "axios";
import { useState } from "react";

export default function Signout(){
    return(
        <div>
            <button className="bg-violet-700">Sign out</button>
        </div>
    );
}